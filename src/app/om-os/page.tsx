import type { Metadata } from "next";
import { ContactBand, PageHero, PageShell } from "@/components/PageShell";
import { siteData } from "@/data/active-site";
export const metadata: Metadata = { title: "Om os | Københavns Trafikskole" };
export default function AboutPage() { return <PageShell><PageHero eyebrow="Om Københavns Trafikskole" title={siteData.about.lead} text={siteData.about.text} aside={<><small>Tilgang</small><strong>Behagelig atmosfære og plads til fejl</strong><p>Undervisningen tilpasses efter skolens egen beskrivelse til den enkelte elev.</p></>} /><section className="section"><div className="shell about-story"><div><p className="eyebrow">Skolens historie</p><h2>En mere menneskelig køreskole.</h2></div><div><p>Skolen fortæller, at idéen opstod efter en dårlig køreskoleoplevelse i ejerens egen familie. Ambitionen blev at skabe en bedre ramme med dygtig undervisning, forståelse og tæt dialog.</p><p>På sin offentlige hjemmeside fremhæver skolen erfaring med elever med blandt andet ADHD, autisme og angst. Det formidler vi her som en undervisningstilgang — uden løfter om bestemte resultater.</p><small>{siteData.about.note}</small></div></div></section><section className="section alt-section"><div className="shell"><div className="section-heading"><p className="eyebrow">Kørelærerne</p><h2>Mød holdet.</h2><p className="portrait-note">Ingen portrætter er indsat i previewet uden godkendte billedrettigheder.</p></div><div className="team-grid">{siteData.instructors.map(person => <article key={person.name}><div className="initial" aria-hidden="true">{person.name.charAt(0)}</div><small>{person.role}</small><h3>{person.name}</h3><p>{person.details}</p></article>)}</div></div></section><ContactBand title="Tag en uforpligtende snak." /></PageShell>; }
