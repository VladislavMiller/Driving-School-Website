import type { Metadata } from "next";
import { Arrow, Phone } from "@/components/icons";
import { PageHero, PageShell } from "@/components/PageShell";
import { SignupForm } from "@/components/SignupForm";
import { siteData } from "@/data/active-site";
export const metadata: Metadata = { title: "Kontakt | Københavns Trafikskole" };
export default function ContactPage() { return <PageShell><PageHero eyebrow="Kontakt skolen" title="Lad os finde din vej videre." text="Spørg om næste hold, få en pris afklaret, eller fortæl hvilken undervisning du leder efter." aside={<><small>Telefon</small><strong>{siteData.business.phone}</strong><p>Der er ikke offentliggjort verificerede åbningstider.</p></>} /><section className="section contact-page"><div className="shell contact-page-grid"><div><p className="eyebrow">Direkte kontakt</p><h2>Ring, skriv eller<br /><em>kom forbi.</em></h2><div className="contact-cards"><a href={`tel:${siteData.business.phoneHref}`}><Phone /><span><small>Ring direkte</small><strong>{siteData.business.phone}</strong></span></a><a href={`mailto:${siteData.business.email}`}><span className="at">@</span><span><small>Send e-mail</small><strong>{siteData.business.email}</strong></span></a><div><span className="pin">•</span><span><small>Adresse</small><strong>{siteData.business.address}</strong>{siteData.business.mapUrl && <a href={siteData.business.mapUrl} target="_blank" rel="noreferrer">Åbn kort <Arrow /></a>}</span></div></div><div className="social-row">{siteData.socialLinks.filter(link => link.href).map(link => <a href={link.href!} target="_blank" rel="noreferrer" key={link.label}>{link.label}</a>)}</div></div><div className="form-wrap" id="formular"><p className="eyebrow">Reservation / forespørgsel</p><h3>Hvad kan vi hjælpe med?</h3><SignupForm /></div></div></section></PageShell>; }
