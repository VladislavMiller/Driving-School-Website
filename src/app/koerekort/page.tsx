import type { Metadata } from "next";
import { Arrow } from "@/components/icons";
import { ContactBand, PageHero, PageShell } from "@/components/PageShell";
import { siteData } from "@/data/active-site";
export const metadata: Metadata = { title: "Kørekort og undervisning | Københavns Trafikskole" };
export default function LicencePage() { return <PageShell><PageHero eyebrow="Kørekort og undervisning" title="Et forløb, der passer til dig." text="Få overblik over skolens offentliggjorte muligheder — fra første kategori B-kørekort til generhverv og særlig undervisning." aside={<><small>Mest efterspurgt</small><strong>Manuel, automat eller kombi</strong><a href="/priser">Sammenlign priser <Arrow /></a></>} /><section className="section"><div className="shell"><div className="service-grid">{siteData.services.map((service, i) => <article key={service.title}><span>0{i + 1}</span><small>{service.detail}</small><h2>{service.title}</h2><p>{service.text}</p><div><a href="/priser">Se priser</a><a href="/kontakt#formular">Kontakt skolen <Arrow /></a></div></article>)}</div></div></section><section className="section teaching-note"><div className="shell narrow"><p className="eyebrow">Undervisning med plads</p><h2>{siteData.about.lead}</h2><p>{siteData.about.text}</p><a className="button-link" href="/om-os">Læs om skolen</a></div></section><ContactBand /></PageShell>; }
